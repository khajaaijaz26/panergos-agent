name: openrouter
version: 1.1.0
description: "OpenRouter image generation through chat-completions and the dedicated image API, with text-to-image, image-to-image, and reference-grounded output."
author: Panergos Agent
kind: backend
requires_env:
  - OPENROUTER_API_KEY
