{
  "name": "@panergos/browser-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Panergos Relay side-panel extension for one-prompt browser work.",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "node --check src/background.js && node --check src/controller.js && node --check src/protocol.js && node --check src/sidepanel.js",
    "test": "node --test tests/*.test.mjs"
  }
}
